{
  "name": "playwright-mcp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test --reporter=html"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
